const {onCall,HttpsError}=require('firebase-functions/v2/https');const admin=require('firebase-admin');admin.initializeApp();const db=admin.firestore();
exports.createNetworkEvent=onCall(async req=>{if(!req.auth)throw new HttpsError('unauthenticated','Inicia sesión');const {type,businessId,customerId,metadata={}}=req.data||{};if(!type||!businessId)throw new HttpsError('invalid-argument','Faltan datos');const ref=await db.collection('networkEvents').add({type,businessId,customerId:customerId||null,metadata,createdBy:req.auth.uid,createdAt:admin.firestore.FieldValue.serverTimestamp()});return{id:ref.id};});
